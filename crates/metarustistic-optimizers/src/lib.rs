pub fn add(left: usize, right: usize) -> usize {
    left + right
}
pub mod random
pub fn random(eval_function: fn)->f64{
    let 
    score = 
}
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
